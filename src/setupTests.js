// jest-dom adds custom matchers for asserting on DOM nodes, e.g.:
//   expect(element).toHaveTextContent(/react/i)
// The `/vitest` subpath registers the matchers with Vitest's `expect`.
// (The old `@testing-library/jest-dom/extend-expect` subpath was removed in jest-dom v6;
//  it went unnoticed here because this file was never referenced by a test runner config.)
import '@testing-library/jest-dom/vitest';
